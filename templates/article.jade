
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + ' - '}

block content
  include author
  article.article
    header.header
      h1= page.title
      p.author
        | #{ 'Par ' }
        mixin author(page.metadata.author)
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) « Retour à l'accueil
